import { MarqueeText } from './marquee-text';

describe('MarqueeText component', () => {
  it('is defined and exports MarqueeText', () => {
    expect(MarqueeText).toBeDefined();
    expect(typeof MarqueeText).toBe('function');
  });
});
